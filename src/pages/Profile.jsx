import React from 'react';

export default function Profile(){
  return (
    <div className="page">
      <h2>Profile</h2>
      <p>This is a placeholder profile page. In later iterations we'll load real profiles and show the modal flow.</p>
    </div>
  );
}